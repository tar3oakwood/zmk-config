#include "gravity36_xiao.dtsi"

&kscan0 {
    compatible = "zmk,kscan-gpio-matrix";
};

/ {
    chosen {
        zmk,kscan = &kscan0;
    };
};
&default_transform {
    /* * 右側は物理的に左と同じ配線なので、
     * 「読み取った列番号(col)に5を足せ」という命令で
     * 自動的に右側のキー(RC(0,5)〜)として認識させます
     */
    col-offset = <5>;
};
